# k6 Load Testing Environment Configuration
# Copy this file to .env and update with your values

# Environment to test (local, staging, production)
ENV=local

# Base URL for the application
BASE_URL=http://localhost:3000

# Scenario to run (smoke, load, stress, soak)
SCENARIO=load

# Stripe webhook secret for webhook tests
# Get this from Stripe Dashboard > Developers > Webhooks
STRIPE_WEBHOOK_SECRET=whsec_test_secret

# Test user credentials
# These users should exist in your test database
TEST_USER_EMAIL=loadtest1@example.com
TEST_USER_PASSWORD=LoadTest123!

# Optional: Supabase credentials for test data setup
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: Custom thresholds (override defaults)
# API_P95_THRESHOLD=500
# WEBHOOK_P95_THRESHOLD=200
# DASHBOARD_P95_THRESHOLD=2000

# Optional: Test data configuration
# TEST_ORGANIZATION_COUNT=5
# TEST_CAMPAIGN_COUNT=20
# TEST_PRODUCT_COUNT=10

# Optional: Rate limiting
# MAX_REQUESTS_PER_SECOND=100

# Optional: Output configuration
# RESULTS_DIR=./load-tests/results
# SUMMARY_OUTPUT=json

# Optional: Debugging
# K6_DEBUG=true
# VERBOSE=true
